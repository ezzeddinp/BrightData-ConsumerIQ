# Backend

Local services, model loaders, and data pipelines.

## Local model setup
- Install deps:
	- `pip install -r backend/requirements.txt`
- Embedder sanity check:
	- `python backend/models/embeddings.py`